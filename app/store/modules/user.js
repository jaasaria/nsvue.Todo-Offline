const state = {
	image: 'http://icons.iconarchive.com/icons/paomedia/small-n-flat/256/user-male-icon.png',
};


const mutations = {
	setUserMutation: (state, payload) => {
		// state.loginInfo = payload;
		// state.isLoginState = true
	},

}

const actions = {
	// getImageAction: ({commit}) => commit('getImageMutation'),

};


export default {
  state,
  mutations,
  actions,
};
